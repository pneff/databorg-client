WITH <urn:one:graph>
DELETE { ?s <foo:bar> ?o }
USING <urn:another:graph>
USING NAMED <urn:anamed:graph>
WHERE { ?s <foo:bar> ?o }

# replace ?o with example url
###variables: {"o": "<http://example.org>"}
